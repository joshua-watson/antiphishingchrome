{
  "name": "phishing-detector",
  "version": "1.0.0",
  "description": "A Chrome extension to detect phishing emails using TensorFlow.js.",
  "scripts": {
    "build": "parcel build src/manifest.json src/popup.html --dist-dir dist",
    "clean-cache": "parcel clean"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "parcel": "^2.12.0",
    "process": "^0.11.10",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^3.21.0"
  }
}
